<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to t("brand"), root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= active_menu 'static_pages', 'index' %>">
          <%= link_to t("menu.home"), root_path %></li>
        <% Category.all.each do |category| %>
          <li class="<%= active_menu 'categories', 'show', category.id %>">
            <%= link_to category.name, category %></li>
        <% end %>
        <li class="<%= active_menu 'promotions', 'index' %>">
          <%= link_to t("menu.promotion"), promotions_path %></li>
        <li class="<%= active_menu 'static_pages', 'contact' %>">
          <%= link_to t("menu.contact"), contact_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to product_carts_path, class: "cart" do %>
            <span><%= image_tag "cart.png" %></span>
            <span class="badge number-cart"><%= number_cart %></span>
          <% end %>
        </li>
        <li class="search">
          <a class="popup-with-zoom-anim search" href="#small-dialog">
          <i class="glyphicon glyphicon-search"></i></a>
        </li>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"
              role="button" aria-haspopup="true" aria-expanded="false">
              <%= current_user.name %><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to t("menu.orders"), invoices_path %></li>
              <li><%= link_to t("menu.profile"), edit_user_registration_path %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to t("menu.sign_out"),  destroy_user_session_path,
                method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t("menu.login"), new_user_session_path %></li>
          <li><%= link_to t("menu.register"), new_user_registration_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
