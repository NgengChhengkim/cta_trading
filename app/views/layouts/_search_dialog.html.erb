<div id="small-dialog" class="mfp-hide">
  <div class="search-top">
    <div class="login">
      <%= search_form_for Product.ransack(params[:q]), url: search_path do |f| %>
        <%= f.submit :submit, value: "" %>
        <%= f.search_field :name_or_code_cont, class: "search-text",
          placeholder: t("labels.search"), autocomplete: "off" %>
      <% end %>
    </div>
  </div>
</div>
