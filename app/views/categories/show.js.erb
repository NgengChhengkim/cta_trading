$(".category-products").append("<%= j render 'shared/products' %>");
$(".loading").hide();
$(".btn-load-more").show();
<% if @products.next_page %>
  $(".pagination").html("<%= j will_paginate @products %>");
<% else %>
  $(".load-more").remove();
<% end %>
