$(".category-products").append("<%= j render partial: 'shared/products', collection: @products, as: :product %>");
$(".loading").hide();
$(".btn-load-more").show();
<% if @products.next_page %>
  $(".pagination").html("<%= j will_paginate @products %>");
<% else %>
  $(".load-more").remove();
<% end %>
